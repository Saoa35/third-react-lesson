import './App.css';


const names = ['Alice', 'Kim', 'Julian', 'Alex', 'Tatiana'];
const arrElements = names.map(item => <li>{item}</li>);

function App() {
  return (
    <div className="App">
      
    </div>
  );
}

export default App;
